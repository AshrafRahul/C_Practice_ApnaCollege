#include<stdio.h>

int main(){
    char name[20], varsity[20];
    scanf("%s", &name);
    scanf("%s", &varsity);

    printf("Name: %s\n", name);
    printf("Varsity: %s\n", varsity);
    return 0;
}